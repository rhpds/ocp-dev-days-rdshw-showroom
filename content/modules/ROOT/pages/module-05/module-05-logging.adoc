= logging.yaml
include::partial$_attributes.adoc[]

Custom MTA **rules** that detect `audit-logging-library` v1 usage and guide migration to v2. Save this file as `rules/logging.yaml` in `parasol-insurance-eap`.

[.button]
* link:{attachmentsdir}/module-05-rules/logging.yaml[Download logging.yaml,download=logging.yaml,window=_blank,role=button]

TIP: Use the download link above, or copy the YAML below into a local file and save it on your local computer in a rules directory e.g. `rules/logging.yaml`.

[.console-input]
[source,yaml]
----
include::example$module-05-rules/logging.yaml[]
----

Back to xref:module-05/module-05-05-custom-rules-audit-logging.adoc[Section 5: Custom rules (audit logging)].
